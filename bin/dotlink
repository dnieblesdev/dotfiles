#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

export DOTLINK_REPO_ROOT="${DOTLINK_REPO_ROOT:-$REPO_ROOT}"

exec "$DOTLINK_REPO_ROOT/dotlink/dotlink.sh" "$@"
